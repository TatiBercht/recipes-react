import { sortFunction } from '../../src/lib/array-helpers'

describe('Array helpers', () => {
    describe('sort by', () => {
        it('sorts by title', () => {
            console.log('hello')
        })
    })
})